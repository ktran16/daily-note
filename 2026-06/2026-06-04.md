# Daily Notes - 2026-06-04

## 00:03 - dbt Incremental Models: Building Efficient Data Pipelines

### What Are dbt Incremental Models?

In data engineering, **dbt (data build tool)** is a transformation framework that lets analysts write modular SQL. One of its most powerful features is **incremental models** — a strategy for processing only new or changed data rather than rebuilding entire tables on every run.

### The Problem They Solve

Full-refresh models re-process every row in a source table each time they run. For tables with millions or billions of rows, this is prohibitively slow and expensive, particularly in cloud data warehouses billed by compute or bytes scanned (BigQuery, Snowflake, Redshift).

### How Incremental Models Work

An incremental model wraps standard SQL with a conditional filter:

```sql
{{ config(materialized='incremental', unique_key='event_id') }}

SELECT
    event_id,
    user_id,
    event_type,
    created_at
FROM {{ source('raw', 'events') }}

{% if is_incremental() %}
  WHERE created_at > (SELECT MAX(created_at) FROM {{ this }})
{% endif %}
```

On the **first run**, dbt ignores the `is_incremental()` block and loads the full dataset. On **subsequent runs**, only rows newer than the current maximum `created_at` are fetched and merged/inserted.

### Key Configuration Options

| Strategy | Behavior |
|---|---|
| `append` | Inserts new rows only; no deduplication |
| `merge` | Upserts rows matched on `unique_key` |
| `delete+insert` | Deletes matching rows then reinserts |
| `insert_overwrite` | Replaces entire partitions (BigQuery/Spark) |

### Practical Tips

- **Always define a `unique_key`** when using `merge` to avoid duplicate rows during reruns or backfills.
- **Use event timestamps** (e.g., `created_at`, `updated_at`) as your incremental filter — avoid surrogate keys which don't signal recency.
- **Run `dbt build --full-refresh`** periodically or after schema changes to resync the full table state.
- **Partition your target table** by the timestamp column to make incremental scans dramatically cheaper in BigQuery and Snowflake.

### When Not to Use Incremental Models

Avoid incremental models when source data is frequently updated in-place (late-arriving facts, corrections) without a reliable `updated_at` column — in those cases, a full-refresh or snapshot strategy is safer and avoids silent data drift.

Incremental models are a cornerstone of cost-efficient, production-grade data pipelines and are worth mastering early in any data engineering practice.

---

## 05:03 - RAG vs. Fine-Tuning: Choosing the Right LLM Adaptation Strategy

### The Core Question

When a base large language model (LLM) doesn't know your domain — your internal docs, product catalog, or proprietary data — you have two primary options: **Retrieval-Augmented Generation (RAG)** or **fine-tuning**. They solve different problems, and confusing them leads to expensive mistakes.

### Retrieval-Augmented Generation (RAG)

RAG keeps the base model frozen and injects relevant context at inference time. A retriever (typically a vector database like Pinecone, Weaviate, or pgvector) finds the top-k most relevant chunks from your knowledge base and stuffs them into the prompt alongside the user's query.

**When RAG wins:**
- Your knowledge base changes frequently (product prices, support tickets, live docs)
- You need source citations for auditability
- Your data volume is too large to fit in a fine-tuning dataset
- You want to avoid retraining costs when information updates

**RAG's weakness:** It fails when the model lacks the *reasoning pattern* or *output format* your task requires — it can't teach a model a new writing style or domain-specific logic through context alone.

### Fine-Tuning

Fine-tuning updates the model's weights on curated examples of inputs and desired outputs. The model internalizes patterns, tone, and domain-specific reasoning.

**When fine-tuning wins:**
- You need a consistent response style or format (structured JSON output, medical report templates)
- The task requires reasoning the base model consistently gets wrong
- Latency matters and you can't afford large context windows at inference time
- Your training data is stable and unlikely to change often

**Fine-tuning's weakness:** It can't inject facts the training data didn't cover. A fine-tuned model will still hallucinate on questions outside its training distribution.

### The Practical Heuristic

| Need | Solution |
|---|---|
| Up-to-date facts | RAG |
| Consistent format / style | Fine-tuning |
| Both | RAG + fine-tuning together |
| Neither helps | Rethink the task decomposition |

Start with RAG — it's faster to iterate and cheaper to maintain. Graduate to fine-tuning only once you've confirmed the bottleneck is model behavior, not missing context.

---

## 10:03 - Bitcoin's Lightning Network: How Layer 2 Payments Work

### The Scalability Problem

Bitcoin's base layer processes roughly **7 transactions per second** — a fundamental constraint of its 1 MB block size and 10-minute block intervals. For comparison, Visa handles ~24,000 TPS. This bottleneck makes micropayments (tipping, pay-per-use APIs, streaming payments) economically unviable on-chain when fees spike during congestion.

The **Lightning Network** solves this by moving the vast majority of transactions off-chain, settling only the net result on Bitcoin's blockchain.

### How Payment Channels Work

Two parties open a **payment channel** by locking funds in a 2-of-2 multisignature Bitcoin transaction (the "funding transaction") that gets recorded on-chain. From that point, they can transact with each other indefinitely by exchanging signed **commitment transactions** — cryptographically valid Bitcoin transactions that update the balance split between them — without broadcasting anything to the blockchain.

When they're done, either party broadcasts the final commitment transaction, and the blockchain settles the closing balances. Hundreds or thousands of transactions collapse into just two on-chain footprints: open and close.

### Routing Across the Network

You don't need a direct channel with every person you pay. Lightning uses **Hash Time-Locked Contracts (HTLCs)** to route payments through intermediary nodes:

1. The recipient generates a random secret `R` and shares its hash `H(R)` with the sender.
2. The sender locks funds along a path: "pay the next hop **only if** they reveal the preimage of `H(R)` within X blocks."
3. Each hop passes the condition forward until the recipient reveals `R`, unlocking funds backward along the path.

If any hop fails, the time lock expires and all funds return to their origin — **no counterparty risk for failed routes**.

### Practical Tradeoffs

| Advantage | Limitation |
|---|---|
| Near-instant settlement (milliseconds) | Requires online presence to receive payments |
| Sub-cent fees at scale | Channel capacity constrains max payment size |
| Privacy — intermediate hops don't see full path | Liquidity must be pre-allocated in channels |
| No on-chain footprint per transaction | Routing large payments over low-capacity networks can fail |

### Real-World Adoption

Lightning is live with wallets like **Phoenix**, **Breez**, and **Muun**, and is integrated into platforms like Strike and Cash App. El Salvador's Chivo wallet uses Lightning for everyday purchases. The network currently routes hundreds of millions of dollars in monthly volume with median fees under 1 satoshi per transaction.

Lightning demonstrates a general pattern in blockchain scaling: **use the base layer for settlement and dispute resolution; do the work off-chain**.

---

## 15:03 - XGBoost vs LightGBM: Choosing Your Gradient Boosting Framework

### What Is Gradient Boosting?

Gradient boosting builds an ensemble of **shallow decision trees sequentially**, where each new tree corrects the residual errors of all previous trees. The result is one of the most powerful and widely used algorithms in tabular machine learning — consistently dominating Kaggle competitions and production ML pipelines alike.

**XGBoost** (2014) and **LightGBM** (2017) are the two dominant implementations. They share the same core algorithm but differ significantly in how they grow trees and handle large datasets.

### How They Differ Internally

| | XGBoost | LightGBM |
|---|---|---|
| Tree growth | Level-wise (breadth-first) | Leaf-wise (best-first) |
| Large datasets | Slower; scans all features | Faster via **GOSS** + **EFB** |
| Categorical features | Requires manual encoding | Native categorical support |
| Memory usage | Higher | Lower (histogram binning) |
| Accuracy on small data | Often slightly better | Comparable or better with tuning |

**Leaf-wise growth** (LightGBM) expands the single leaf with the highest loss reduction at each step, creating asymmetric, deeper trees faster. This makes LightGBM significantly faster on large datasets but more prone to overfitting on small ones.

**GOSS (Gradient-based One-Side Sampling)** discards samples with small gradients (already well-learned) and keeps those with large gradients, preserving informativeness while reducing computation. **EFB (Exclusive Feature Bundling)** merges sparse mutually exclusive features, reducing the effective feature count.

### Practical Decision Guide

**Use LightGBM when:**
- Your dataset has > 100K rows — training speed is 5–10× faster
- You have high-cardinality categorical features (use `categorical_feature` param)
- Memory is constrained

**Use XGBoost when:**
- Your dataset is small (< 50K rows) — leaf-wise growth overfits more easily
- You need battle-tested monotone constraints or better SHAP integration
- You want native GPU training on sparse data

### Key Hyperparameters That Actually Matter

Both frameworks share conceptually similar knobs:
- **`n_estimators` + `learning_rate`**: Always tune together — more trees with lower LR generally outperforms fewer trees with high LR.
- **`max_depth` / `num_leaves`**: Controls tree complexity; `num_leaves` in LightGBM is the primary lever (keep < 2^max_depth).
- **`subsample` + `colsample_bytree`**: Row and column subsampling add regularization and reduce variance.
- **`min_child_samples` (LightGBM)**: Minimum data per leaf — raise this first when overfitting on small data.

Both are excellent defaults for structured data. Start with LightGBM for speed, validate with cross-validation, and reach for XGBoost if you need tighter control on small or sparse datasets.

---

## 19:03 - Cohort Analysis: Measuring User Retention the Right Way

### What Is a Cohort?

A **cohort** is a group of users who share a common characteristic within a defined time window — most often their acquisition date (the week or month they first signed up). Cohort analysis tracks how these groups behave over time, making it possible to compare retention, revenue, or engagement across different generations of users without mixing signals from newer and older cohorts.

### Why Aggregate Metrics Lie

Suppose your DAU (Daily Active Users) is flat for three months. That could mean retention is healthy — or it could mean you're churning existing users as fast as you acquire new ones. Aggregate metrics can't tell you which. Cohort analysis can.

By isolating each acquisition cohort, you reveal patterns invisible in averages: a product change in March may have dramatically improved retention for cohorts acquired after that date while earlier cohorts continue churning at the old rate.

### Reading a Retention Triangle

The standard cohort output is a **retention triangle** — a matrix where:
- Rows = cohorts (e.g., "users acquired in Jan 2026")
- Columns = periods since acquisition (Day 1, Day 7, Day 30…)
- Cells = % of the cohort still active at that period

```
Cohort     | Day 1 | Day 7 | Day 30 | Day 90
-----------|-------|-------|--------|-------
Jan 2026   |  100% |  45%  |  22%   |  11%
Feb 2026   |  100% |  48%  |  26%   |  13%
Mar 2026   |  100% |  52%  |  31%   |  17%
```

An upward trend down the column (Day 30 improving from 22% → 31%) signals that a product or onboarding change improved long-term retention — one of the most valuable signals a growth team can find.

### SQL Pattern for Cohort Retention

```sql
WITH cohorts AS (
  SELECT user_id, DATE_TRUNC('month', first_seen_at) AS cohort_month
  FROM users
),
activity AS (
  SELECT user_id, DATE_TRUNC('month', event_at) AS activity_month
  FROM events
)
SELECT
  c.cohort_month,
  DATE_DIFF('month', c.cohort_month, a.activity_month) AS period,
  COUNT(DISTINCT a.user_id) * 1.0 / COUNT(DISTINCT c.user_id) AS retention_rate
FROM cohorts c
JOIN activity a USING (user_id)
GROUP BY 1, 2
ORDER BY 1, 2;
```

### Practical Pitfalls

- **Don't mix acquisition channels** in a single cohort — paid users and organic users behave differently; cohort them separately to avoid Simpson's paradox.
- **Use fixed cohort windows** (7-day, 30-day, 90-day) rather than calendar periods when cohort sizes vary wildly month-to-month.
- **Watch for survivorship bias** — if you only analyze retained users when building features, your model will learn to predict the already-retained, not the at-risk.

Cohort analysis is the single most reliable tool for distinguishing between a product that is genuinely improving and one that is merely growing its way out of a retention problem.
