# Daily Notes - 2026-07-12

## 00:02 - dbt Python Models: Running Python Logic Inside Your dbt DAG

dbt Python models (`.py` files) let you run Python code as a first-class node in your dbt DAG — useful when SQL falls short: ML inference, complex string parsing, pandas-style reshaping, or calling external APIs during transformation.

### When to Use Python Models

- Applying a trained ML model to a table of features
- Deduplication or fuzzy matching with libraries like `rapidfuzz`
- Multi-step dataframe transformations that would be awkward in SQL window functions
- Calling external REST APIs and persisting the results

They are currently supported on **Snowflake** (Snowpark), **Databricks** (PySpark / pandas-on-Spark), **BigQuery** (Dataproc), and **Amazon Athena** (Spark).

### Anatomy of a Python Model

```python
# models/ml/scored_customers.py

def model(dbt, session):
    # Reference upstream dbt models or sources
    customers = dbt.ref("stg_customers")
    orders    = dbt.ref("fct_orders")

    # Use native DataFrame API (Snowpark / PySpark)
    df = customers.join(orders, on="customer_id", how="left")
    df = df.with_column("clv_score", df["lifetime_value"] * 0.85)

    return df
```

The `model(dbt, session)` signature is required. `dbt.ref()` and `dbt.source()` return native DataFrame objects for the active platform.

### Config Stays the Same

```python
def model(dbt, session):
    dbt.config(
        materialized="table",
        packages=["scikit-learn==1.4.0"],  # Snowflake: install in the session
    )
    ...
```

All standard dbt configs — `materialized`, `tags`, `meta`, `grants` — work identically to SQL models.

### Mixing SQL and Python in One DAG

Python models are full DAG citizens. A downstream SQL model can `ref()` a Python model and vice-versa:

```sql
-- models/marts/enriched_customers.sql
select *
from {{ ref('scored_customers') }}   -- Python model upstream
where clv_score > 0.7
```

### Key Limitations

| Constraint | Detail |
|---|---|
| Materialization | Only `table` and `incremental` (no `view` — Python runs in a session, not a query) |
| Incremental logic | Use `dbt.is_incremental()` and filter the input DataFrame manually |
| Testing | Same generic/singular tests apply to the output table |
| IDE linting | SQL linters won't help; use `mypy` or `ruff` in a separate pre-commit step |

### Incremental Python Model Pattern

```python
def model(dbt, session):
    dbt.config(materialized="incremental", unique_key="event_id")

    df = dbt.ref("raw_events")

    if dbt.is_incremental():
        # Only process events newer than the latest already loaded
        max_ts = session.sql(
            f"select max(event_ts) from {dbt.this}"
        ).collect()[0][0]
        df = df.filter(df["event_ts"] > max_ts)

    return df
```

### Best Practices

- Keep Python models **thin** — do heavy lifting in SQL upstream, use Python only for what SQL can't do.
- Pin library versions in `dbt.config(packages=[...])` to avoid silent breakage across runs.
- Store shared Python helpers in `macros/` as `.py` files (dbt 1.8+) or in a separate `utils` package.
- Python models are slower to run than SQL models (session spin-up overhead), so avoid using them for simple transformations.

---

## 05:02 - dbt Model Contracts: Enforcing Schema Integrity Across Model Boundaries

dbt Model Contracts (stable since dbt 1.5) let you declare a model's expected columns, data types, and constraints as part of the model definition itself — turning your schema into a **compile-time and run-time guarantee** rather than a hope.

### Why Contracts Matter

Without contracts, a model's schema is purely implicit. A refactor that renames a column silently breaks every downstream consumer. Contracts make that break **explicit and immediate**, catching it before bad data reaches production.

### Enabling a Contract

Add `contract: {enforced: true}` to your model config and declare each column with its data type:

```yaml
# models/marts/fct_orders.yml
models:
  - name: fct_orders
    config:
      contract:
        enforced: true
    columns:
      - name: order_id
        data_type: bigint
        constraints:
          - type: not_null
          - type: primary_key
      - name: customer_id
        data_type: bigint
        constraints:
          - type: not_null
      - name: order_total
        data_type: numeric
      - name: ordered_at
        data_type: timestamp
```

When `enforced: true`, dbt will **fail at compile time** if the model's SELECT list doesn't return all declared columns, and some adapters (Snowflake, BigQuery, Redshift, Postgres) enforce the `not_null` and `primary_key` constraints at the database level.

### Supported Constraint Types

| Constraint | Adapter enforcement | dbt-level check |
|---|---|---|
| `not_null` | Yes (most adapters) | Yes |
| `primary_key` | Varies (some adapters create PK) | Yes (uniqueness not guaranteed) |
| `unique` | Varies | No — use generic `unique` test |
| `foreign_key` | Varies | No — use relationships test |
| `check` | Varies | No |
| `custom` | Adapter-defined | No |

### Contracts + Versioned Models

Contracts shine brightest with **model versioning** (`v1`, `v2`, …). A version bump signals to consumers that a breaking change happened; the contract documents exactly what changed:

```yaml
models:
  - name: fct_orders
    latest_version: 2
    versions:
      - v: 1
        config:
          contract: {enforced: true}
      - v: 2
        config:
          contract: {enforced: true}
        columns:
          - name: order_id
            data_type: bigint
          - name: order_total_usd   # renamed from order_total
            data_type: numeric
```

Downstream models using `ref('fct_orders', v=1)` keep working; only those that explicitly upgrade to `v=2` need to handle the rename.

### Best Practices

- Enforce contracts on **public models** (those consumed by other teams or external tools) — not on every staging model.
- Always pair a `primary_key` constraint with a `unique` + `not_null` dbt test; the constraint documents intent while the test actually validates it.
- Use contracts as the source of truth for documentation: tools like dbt Docs and the semantic layer can read declared column types directly.
- CI should run `dbt compile` on PRs to catch contract violations before merge — no data needs to move, just the DAG needs to parse.

---

## 10:03 - dbt Snapshots: Tracking Slowly Changing Dimensions Over Time

dbt Snapshots implement **Type 2 Slowly Changing Dimensions (SCD2)** — automatically capturing row-level history as source data changes over time. Instead of every query seeing only the current state of a record, snapshots let you ask "what did this record look like on a given date?"

### How Snapshots Work

dbt adds four metadata columns to the snapshot table on each `dbt snapshot` run:

| Column | Meaning |
|---|---|
| `dbt_scd_id` | Unique identifier for this row version (hash) |
| `dbt_updated_at` | Timestamp of when this version was captured |
| `dbt_valid_from` | When this version became active |
| `dbt_valid_to` | When it was superseded (`NULL` = current row) |

### Defining a Snapshot

Snapshots live in the `snapshots/` directory and use a `{% snapshot %}` block:

```sql
-- snapshots/snap_customers.sql

{% snapshot snap_customers %}

  {{
    config(
      target_schema='snapshots',
      unique_key='customer_id',
      strategy='timestamp',
      updated_at='updated_at',
    )
  }}

  select * from {{ source('app_db', 'customers') }}

{% endsnapshot %}
```

Run with:

```bash
dbt snapshot
```

### Two Snapshot Strategies

**`timestamp`** — compares an `updated_at` column to detect changes. Fast and reliable when the source has a reliable update timestamp.

```yaml
strategy: timestamp
updated_at: updated_at
```

**`check`** — hashes a list of columns and flags a change when any column value differs. Useful when there is no `updated_at` column in the source.

```yaml
strategy: check
check_cols:
  - email
  - phone
  - address_line_1
  - plan_tier
# or: check_cols: all
```

### Querying Snapshot History

```sql
-- Current state only
select * from {{ ref('snap_customers') }}
where dbt_valid_to is null;

-- Point-in-time: what was each customer's plan on 2025-01-01?
select *
from {{ ref('snap_customers') }}
where '2025-01-01'
    between dbt_valid_from and coalesce(dbt_valid_to, current_timestamp);

-- Full history for a specific customer
select *
from {{ ref('snap_customers') }}
where customer_id = 42
order by dbt_valid_from;
```

### Downstream Models Using Snapshot History

A fact table can join to the snapshot using the point-in-time pattern to capture the attribute value **at the time of the event**, not today's value:

```sql
-- models/marts/fct_orders_with_customer_plan.sql
select
    o.order_id,
    o.ordered_at,
    c.plan_tier as plan_tier_at_order_time
from {{ ref('fct_orders') }} o
join {{ ref('snap_customers') }} c
  on  o.customer_id = c.customer_id
  and o.ordered_at between c.dbt_valid_from
                       and coalesce(c.dbt_valid_to, current_timestamp)
```

### Best Practices

- Run snapshots **before** your standard `dbt run` in your pipeline so models always see the freshest history.
- Snapshots are **append-only** — never truncate or rebuild the target table; you will lose historical versions.
- Keep snapshot queries simple (usually `select * from source`). Apply transformations in downstream models that `ref()` the snapshot.
- Set `target_schema` to a separate schema (e.g., `snapshots`) to keep them visually distinct from regular models.
- Add a `dbt_valid_to is null` filter as a default in your BI tool's table definition so analysts always start from the current view.

---

## 15:03 - dbt Node Selection Syntax: Mastering `--select`, `--exclude`, and Graph Operators

dbt's selection syntax is one of its most powerful — and underused — features. Instead of running every model on every `dbt run`, you can target exactly the nodes you care about using a composable set of selectors and graph operators.

### The Basics

```bash
dbt run --select my_model               # single model by name
dbt run --select path/to/models/        # all models in a directory
dbt run --select tag:marketing          # all models with tag: marketing
dbt run --select source:app_db          # all models that select from a source
```

### Graph Operators: `+` and `@`

The `+` operator expands the selection to include **graph neighbors**:

```bash
# Run fct_orders AND all its upstream dependencies
dbt run --select +fct_orders

# Run fct_orders AND everything downstream of it
dbt run --select fct_orders+

# N-degree traversal: only 2 layers upstream
dbt run --select 2+fct_orders

# Both directions: all ancestors and descendants
dbt run --select +fct_orders+
```

The `@` operator is useful in CI — it selects a model plus all its parents **and** all children of those parents (i.e. "everything that shares an ancestor with this model"):

```bash
# Great for CI: rebuild the full subgraph around changed models
dbt run --select @stg_customers
```

### Set Operators: Unions, Intersections, Exclusions

Separate selectors with spaces for a **union** (OR logic):

```bash
dbt run --select fct_orders fct_revenue   # both models
```

Use commas for an **intersection** (AND logic):

```bash
# Models that are BOTH tagged 'nightly' AND in the marts/ folder
dbt run --select tag:nightly,path:models/marts/
```

Use `--exclude` to subtract from any selection:

```bash
# Run all models except the slow ones
dbt run --select tag:daily --exclude tag:slow
```

### Selector Methods

| Method | Example | Matches |
|---|---|---|
| `tag:` | `tag:pii` | Models with that tag |
| `source:` | `source:stripe` | Models sourcing from `stripe` |
| `config:` | `config.materialized:incremental` | Models with that config value |
| `path:` | `path:models/staging/` | Models under that path |
| `package:` | `package:dbt_utils` | Nodes from a package |
| `exposure:` | `exposure:weekly_revenue` | Models feeding an exposure |
| `metric:` | `metric:revenue` | Models feeding a metric |
| `result:` | `result:error` | Nodes that errored in the last run |
| `state:` | `state:modified` | Nodes changed vs. a prior state |

### The `state:` Selector: CI Superpowers

The `state:modified` selector compares your current project against a saved **manifest.json** from a prior run. This enables **slim CI** — only build what actually changed:

```bash
# In CI: download prod manifest.json, then:
dbt build --select state:modified+ --defer --state ./prod-manifest/
```

- `state:modified` — any node whose definition changed (SQL, YAML, upstream ref)
- `state:modified.body` — SQL body changed only (not config/meta changes)
- `state:new` — brand-new nodes that don't exist in the prior state

Combined with `--defer`, missing upstream nodes are served from the production environment rather than rebuilt — fast, cheap CI that doesn't touch your whole warehouse.

### Saving Selectors as YAML

For complex or frequently reused selections, define them in `selectors.yml`:

```yaml
# selectors.yml
selectors:
  - name: nightly_marts
    description: All incremental marts and their dependencies
    definition:
      union:
        - method: tag
          value: nightly
        - method: config.materialized
          value: incremental
```

Then reference by name:

```bash
dbt run --selector nightly_marts
```

### Best Practices

- Tag models by **schedule** (`daily`, `hourly`, `weekly`) and use `--select tag:` in your orchestrator's task definitions — one cron job per cadence.
- In CI, always pair `state:modified+` with `--defer --state` to avoid rebuilding the entire DAG on every PR.
- Use `result:error+` in retry logic: `dbt run --select result:error+` re-runs only failed models and their descendants.
- Prefer named selectors in `selectors.yml` over long `--select` strings in CI scripts — they're documented, reviewable, and reusable.

---

## 19:02 - dbt Exposures: Documenting Downstream Consumers of Your Data Models

**Exposures** are dbt's mechanism for documenting what consumes your transformed data — dashboards, ML models, reports, reverse-ETL tools, or any downstream application. They close the lineage loop: instead of your DAG ending at a dbt model, it visibly extends to the business systems that depend on it.

### Why Exposures Matter

Without exposures, the impact of changing a model is invisible. With them, dbt can tell you: *"if you modify `fct_orders`, these 3 dashboards will break."* This makes impact analysis fast and keeps your data catalog meaningful to non-engineers.

### Defining an Exposure

Exposures live in `.yml` files (typically `models/exposures.yml`):

```yaml
exposures:
  - name: monthly_revenue_dashboard
    type: dashboard
    maturity: high
    url: https://your-bi-tool.com/dashboards/42
    description: >
      Executive dashboard showing monthly revenue by region and product line.
      Refreshed nightly after the dbt production run.
    depends_on:
      - ref('fct_orders')
      - ref('dim_customers')
      - ref('dim_products')
    owner:
      name: Analytics Team
      email: analytics@company.com
```

### Exposure Types

| Type | Use Case |
|---|---|
| `dashboard` | BI tool reports (Tableau, Looker, Metabase) |
| `notebook` | Jupyter or similar analytical notebooks |
| `analysis` | Ad-hoc SQL analyses or one-off reports |
| `ml_model` | Machine learning models consuming dbt output |
| `application` | Operational apps or reverse-ETL pipelines |

### Querying Exposures in the DAG

Run a subset of models that feed a specific exposure:

```bash
# Build everything upstream of the dashboard
dbt build --select +exposure:monthly_revenue_dashboard

# Test only the sources feeding into it
dbt test --select +exposure:monthly_revenue_dashboard
```

### Maturity Levels

Use `maturity: low | medium | high` to signal how stable an exposure is. A `low`-maturity dashboard signals to consumers that underlying models may still change; `high` implies a formal SLA.

### Best Practices

- **One exposure per consumer** — don't bundle unrelated dashboards; granularity enables precise impact analysis.
- **Set `url`** — a direct link turns the dbt docs site into a true portal, bridging data producers and consumers.
- **Assign an `owner`** — when models upstream of an exposure break, you know exactly who to notify.
- **Review exposures in CI** — `dbt ls --select exposure:*` in your pipeline catches dangling `ref()` calls before they reach production.

Exposures are low-effort, high-value metadata. They make your dbt project legible to the whole organization, not just the team that builds it.
