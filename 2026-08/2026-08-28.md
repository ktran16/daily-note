# Daily Notes - 2026-08-28

## 00:03 - [Data Engineering] The Small File Problem: Compaction and File Layout in Iceberg and Delta

Streaming ingestion and frequent micro-batch writes create a quiet killer: thousands of tiny Parquet files. Each one costs a metadata lookup, an object-store GET with its own latency, and a task slot in Spark. A table with 200k files of 1 MB each scans far slower than the same 200 GB in 1,000 files of 200 MB — same bytes, 200x the overhead.

**Diagnose first.** Iceberg exposes metadata tables:

```sql
SELECT file_path, file_size_in_bytes, record_count
FROM db.events.files
ORDER BY file_size_in_bytes ASC LIMIT 20;

-- Rough health check
SELECT COUNT(*) AS files, AVG(file_size_in_bytes)/1024/1024 AS avg_mb
FROM db.events.files;
```

If `avg_mb` is well under ~100, you have a layout problem. Delta's equivalent is `DESCRIBE DETAIL db.events` (`numFiles`, `sizeInBytes`).

**Compact.** Iceberg ships a rewrite procedure; run it on recent partitions rather than the whole table:

```sql
CALL catalog.system.rewrite_data_files(
  table => 'db.events',
  strategy => 'sort',
  sort_order => 'tenant_id, event_ts',
  where => 'event_date >= current_date - INTERVAL 3 DAYS',
  options => map('target-file-size-bytes','536870912',
                 'min-input-files','5')
);
```

Delta's counterpart is `OPTIMIZE db.events WHERE event_date >= ... ZORDER BY (tenant_id)`.

**Then clean up.** Compaction *adds* files — the old ones stay for time travel. Reclaim storage only after your longest-running reader and rollback window have passed:

```sql
CALL catalog.system.expire_snapshots('db.events', TIMESTAMP '2026-08-21 00:00:00', 100);
CALL catalog.system.remove_orphan_files(table => 'db.events', older_than => TIMESTAMP '2026-08-21 00:00:00');
```

**Prevent, don't just repair.** Over-partitioning is the usual root cause: partitioning by `event_ts` at hour granularity across 500 tenants guarantees tiny files. Prefer daily partitions plus Iceberg hidden partitioning (`days(event_ts)`) and let sort order handle selectivity. For streaming writers, trade latency for size — a 5-minute commit interval produces 60x fewer files than 5 seconds.

Schedule compaction as a first-class orchestrated job with its own SLA, not a thing someone remembers to run after the dashboard gets slow.

---

## 05:03 - [Data Analytics] Deduplication in SQL: QUALIFY, Deterministic Tie-Breakers, and the Silent Row Loss

Every warehouse has a table where "one row per customer" is a lie — CDC replays, retried API loads, and late-arriving corrections all leave duplicates. The classic fix is `ROW_NUMBER()`, but the way most people write it quietly drops or duplicates rows.

**The standard pattern.** On Snowflake, BigQuery, and DuckDB you can skip the subquery entirely with `QUALIFY`:

```sql
SELECT *
FROM raw.orders
QUALIFY ROW_NUMBER() OVER (
          PARTITION BY order_id
          ORDER BY updated_at DESC, ingested_at DESC, _file_row_id DESC
        ) = 1;
```

On Postgres or MySQL 8, wrap it instead:

```sql
WITH ranked AS (
  SELECT *, ROW_NUMBER() OVER (
    PARTITION BY order_id
    ORDER BY updated_at DESC, ingested_at DESC, _file_row_id DESC) AS rn
  FROM raw.orders
)
SELECT * FROM ranked WHERE rn = 1;
```

**The bug: non-deterministic ordering.** If you order only by `updated_at` and two rows share the same second, the engine picks either one — and picks differently on each run. A table with 4.2M orders where 0.3% of `order_id`s have a timestamp tie means ~12,600 rows can flip between builds, so your daily revenue total drifts by a few hundred dollars with no code change. Always end the `ORDER BY` with something unique (a surrogate key, file+row offset, or `_airbyte_raw_id`). That third column is not decoration; it is what makes the query reproducible.

**Pick the right function.** `ROW_NUMBER` returns exactly one row per key. `RANK` returns *all* tied rows — using it here reintroduces the duplicates you were removing. `DISTINCT` only helps when entire rows are identical, which CDC output rarely is.

**Verify before you trust it.** Cheap assertion that survives refactors:

```sql
SELECT order_id, COUNT(*) AS n
FROM analytics.orders_deduped
GROUP BY 1 HAVING COUNT(*) > 1;
```

Zero rows, or the model is wrong. Also compare `COUNT(DISTINCT order_id)` in source vs. target — they must match exactly. If the deduped count is *lower*, your partition key is too coarse and you are deleting real orders, not duplicates.

---

## 10:03 - [ML & AI] Prefill vs. Decode: Why LLM Latency Has Two Very Different Halves

LLM inference is not one workload — it's two, with opposite bottlenecks. Confusing them leads to optimizations that do nothing.

**Prefill** processes the whole prompt in one parallel forward pass, building the KV cache. It's **compute-bound**: the GPU has thousands of tokens of work to chew on at once. It determines **time to first token (TTFT)**.

**Decode** generates output one token at a time. Each step must re-read *every model weight* from HBM to produce a single token, so it's **memory-bandwidth-bound**, and arithmetic units sit mostly idle. It determines **time per output token (TPOT)**.

### The arithmetic

A forward pass costs roughly `2 × N_params` FLOPs per token. For a 7B model with a 4,000-token prompt on a GPU delivering ~150 effective TFLOP/s in bf16:

```
prefill_flops = 2 * 7e9 * 4000      = 5.6e13
TTFT          = 5.6e13 / 150e12     ≈ 0.37 s
```

Decode is bounded by weight reads, not FLOPs. At bf16, 7B params = 14 GB, on a GPU with ~2,000 GB/s bandwidth:

```
TPOT      = 14e9 / 2000e9           ≈ 7 ms/token  (~143 tok/s)
500 tokens                          ≈ 3.5 s
```

Total ≈ 3.9 s — and **90% of it is decode**. Trimming that 4,000-token prompt in half saves ~0.18 s. Trimming 250 output tokens saves ~1.75 s.

```python
def latency(n_params, prompt_tok, out_tok, tflops=150, gbps=2000, bytes_per_param=2):
    ttft = (2 * n_params * prompt_tok) / (tflops * 1e12)
    tpot = (n_params * bytes_per_param) / (gbps * 1e9)
    return ttft, tpot, ttft + tpot * out_tok
```

### What this changes

- **Ask for shorter outputs.** Structured JSON over prose, "answer in one sentence," no restated reasoning — output length is the dominant lever.
- **Batching fixes throughput, not TTFT.** Weights get read once and amortized across the batch, so decode tok/s scales nearly free. It does not make one request faster.
- **Prompt caching attacks the prefill half only** — huge for long fixed system prompts and RAG contexts, near-useless if your latency is decode-dominated.
- **Quantization to int8/int4 speeds up decode directly** by halving or quartering bytes read per token.

Measure TTFT and TPOT separately. A single p95 latency number hides which half you need to fix.

---

## 15:07 - [Crypto & Business] Token Unlock Schedules: Reading FDV, Cliffs, and Emission Curves

Most token launches trade a small slice of supply. The rest arrives later on a published schedule — and that schedule is usually the single most consequential number in a token's design.

**Circulating vs. fully diluted.** Circulating market cap = price × tokens currently liquid. Fully diluted valuation (FDV) = price × total eventual supply. A token at $2.00 with 100M circulating and 1B total supply has a $200M market cap and a **$2B FDV**. The gap is a claim on future supply that existing holders will absorb.

**The float ratio matters.** 10% float means 90% of supply is contractually promised to insiders, investors, treasury, and emissions. Price discovery on 10% of supply says little about what the market clears at when the other 90% arrives.

**Cliffs and linear vesting.** A typical private-round term: *12-month cliff, 24-month linear monthly vest.* Nothing unlocks for 12 months; then 1/24th of that allocation releases monthly. If investors hold 200M tokens, month 13 releases ~8.3M tokens — and every month after. Compare that number to average daily volume. If daily volume is 3M tokens, a monthly 8.3M unlock is roughly 2.8 days of total volume hitting a market that must find new buyers.

**Where to check, not guess.** Unlock schedules live in the project's tokenomics docs and, crucially, in the vesting contract itself. Read the contract on a block explorer: `cliff`, `start`, `duration`, and `released` are usually public view functions. Trackers (TokenUnlocks, CryptoRank) aggregate this, but the contract is ground truth — docs and dashboards go stale, deployed bytecode does not.

**Two red flags in the mechanics:**
- **No on-chain vesting.** If "locked" tokens sit in a multisig with no time-lock contract, they are locked by promise only.
- **Emissions that pay for their own liquidity.** If staking or LP rewards are the main emission sink, TVL is renting yield and leaves when emissions taper.

Read the supply curve before the price chart. One is a plan; the other is a snapshot.

*Educational content only — not financial advice.*

---

## 19:02 - [PC Hardware] Motherboards: What a Higher Tier Actually Buys You (and What It Doesn't)

Motherboards are where build budgets quietly leak. The board does not make your CPU faster — it decides whether the CPU can *sustain* its rated behavior, and which ports you get. Everything else is marketing.

**The chipset picks your features, not your speed.** On AM5, the letter tier maps to connectivity: `B650` gives PCIe 5.0 storage with a 4.0 GPU slot, `B650E`/`X670E`/`X870E` guarantee PCIe 5.0 to the GPU slot, and X-tier adds a second chipset die for more downstream USB and SATA. On Intel's LGA1851, `Z890` unlocks CPU multiplier and memory overclocking, `B860` allows memory overclocking but not CPU, and `H810` locks both and drops to two DIMM slots. That memory-overclock distinction is the one that matters most, because on both platforms all "fast RAM" is an XMP/EXPO overclock — a board that can't do it runs your DDR5-6000 kit at the JEDEC 4800 baseline.

**VRM: read measured temperatures, not phase counts.** "16+2 phases" on a box tells you nothing; four doubled phases of 50A stages behave differently from twelve true 60A stages, and marketing counts both as sixteen. What actually matters is sustained VRM temperature under an all-core load, which only reviews measure. Rough guidance:

| CPU class | VRM concern |
|---|---|
| 6–8 core, ≤125 W sustained | Essentially any B-tier board is fine |
| 12–16 core, 200 W+ PPT/PL2 | Check reviewed VRM temps; want heatsinked stages |
| Sustained all-core (renders, compiles, ML) | Airflow over the VRM heatsink matters as much as the board |

A throttling VRM shows up as clocks decaying several minutes into a render, not as a crash.

**The spec nobody reads: DIMM topology.** Two-slot boards use a shorter daisy-chain trace and routinely clock higher than four-slot boards. On AM5, two DIMMs at DDR5-6000 is the sweet spot; populating all four slots often forces you down to 3600–4400 regardless of kit rating. If you want 96 GB, buy it as 2×48 GB, not 4×24 GB.

Buy the board for its rear I/O, M.2 count, and VRM reviews. Spend the difference on the GPU.
