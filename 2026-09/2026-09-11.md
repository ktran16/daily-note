# Daily Notes - 2026-09-11

## 00:04 - [Data Engineering] Bridge Tables: The Weighting Factor That Stops Many-to-Many Double Counting

Kimball's star schema assumes every fact row points at exactly one row per dimension. Reality disagrees: a bank account has several owners, a support ticket carries several tags, a hospital claim lists several diagnoses. Join the dimension on directly and you fan out — one $1,000 account with three owners becomes three $1,000 rows, and your total deposits triple.

A **bridge table** absorbs the many-to-many. Its grain is one row per (group, member):

```sql
-- dim_account.owner_group_key -> bridge
create table bridge_account_owner (
  owner_group_key  bigint,
  customer_key     bigint,
  weighting_factor numeric(10,6)   -- sums to 1.0 within each group
);
-- group 42: three equal owners
-- (42, 101, 0.333333), (42, 102, 0.333333), (42, 103, 0.333334)
```

The weighting factor is the whole point. It gives you **two legitimate answers** to the same question, and you must pick deliberately:

```sql
-- Impact reporting: "how much balance does customer 101 touch?"
-- No weight. Does NOT sum to the grand total. That's correct, not a bug.
select c.customer_name, sum(f.balance)
from fct_account_balance f
join bridge_account_owner b on f.owner_group_key = b.owner_group_key
join dim_customer c        on b.customer_key = c.customer_key
group by 1;

-- Allocated reporting: "split balance across owners."
-- Sums exactly to the grand total.
select c.customer_name, sum(f.balance * b.weighting_factor)
from fct_account_balance f
join bridge_account_owner b on f.owner_group_key = b.owner_group_key
join dim_customer c        on b.customer_key = c.customer_key
group by 1;
```

Two rules keep this honest. First, the weights are a **business decision** — equal split, ownership percentage, revenue share — so document them in the model's YAML description, not in a stakeholder's head. Second, guard the invariant with a dbt singular test, because drifting weights silently distort every allocated metric:

```sql
-- tests/assert_bridge_weights_sum_to_one.sql
select owner_group_key, sum(weighting_factor) as total
from {{ ref('bridge_account_owner') }}
group by 1
having abs(sum(weighting_factor) - 1.0) > 0.0001
```

If a BI tool can't express the weighted join, build two marts — `_impact` and `_allocated` — and name them so nobody averages them together.

---

## 05:04 - [Data Analytics] Set Operations for Data Diffs: Why `EXCEPT` Says Two Tables Match When They Don't

The standard migration sign-off: rebuild a table on a new pipeline, then prove the output is identical.

```sql
select * from legacy.orders except select * from new.orders;   -- 0 rows
select * from new.orders except select * from legacy.orders;   -- 0 rows
```

Two empty results, ship it. This check is weaker than it looks, in three ways.

**1. `EXCEPT` deduplicates.** It is a *set* operator: both sides are collapsed to distinct rows before the difference. If legacy has one order row three times (a fan-out bug you were hoping to catch) and the new table has it once, both directions return zero rows. A 10,000,001-row table and a 10,000,003-row table pass cleanly. `EXCEPT ALL` keeps multiplicity and is the operator you actually want — available in Postgres, DuckDB and Spark, but not in Snowflake, where `MINUS`/`EXCEPT` are distinct-only.

**2. It matches columns by position, not name.** `select *` on two tables whose `shipped_at` and `ordered_at` columns were swapped during a refactor compares timestamp to timestamp without complaint. Always enumerate columns explicitly on both sides.

**3. It hides *where* the difference is.** Empty is a pass; non-empty tells you a row differs, not which column.

A portable diff that survives all three — group to counts, then full-outer-join on the key:

```sql
with l as (select order_id, status, amount, count(*) n from legacy.orders group by 1,2,3),
     r as (select order_id, status, amount, count(*) n from new.orders    group by 1,2,3)
select coalesce(l.order_id, r.order_id) as order_id,
       l.n as legacy_n, r.n as new_n,
       l.status, r.status, l.amount, r.amount
from l full outer join r
  on  l.order_id is not distinct from r.order_id
  and l.status   is not distinct from r.status
  and l.amount   is not distinct from r.amount
where l.n is distinct from r.n;
```

`IS NOT DISTINCT FROM` matters: `=` returns NULL when either side is NULL, so plain joins drop exactly the rows most likely to be wrong. `EXCEPT` already treats NULLs as equal — switching to a join silently changes that semantic unless you switch the comparison too.

Run the diff on a hashed subset first (`where mod(abs(hash(order_id)), 100) = 0`) to keep the full-table version for the final pass.

---

## 10:03 - [ML & AI] RAG Chunking: Embed Small, Return Big

Chunk size is the most consequential RAG parameter, and it is set before any model runs. The trap: an embedding model compresses a chunk into one fixed-length vector (say 1536 floats) *regardless of input length*. Feed it 100 tokens and the vector describes them precisely. Feed it 1,000 tokens covering five topics and the vector lands at their centroid — a point that describes none of them well. Large chunks quietly destroy retrieval precision.

But small chunks fail the other side: "It was discontinued in Q3" retrieves perfectly and tells the generator nothing, because the subject is three chunks back.

**Small-to-big** resolves the tension. Embed small chunks for matching; return their larger parent for generation.

```python
# index: 256-token children, each carrying a pointer to its parent
children = [
    {"id": c_id, "text": child_text, "parent_id": p_id, "vec": embed(child_text)}
    for p_id, parent in parents.items()
    for c_id, child_text in split(parent, size=256)
]

# query: match on children, deduplicate to parents, send those
hits = vector_search(embed(query), k=12)
parent_ids = list(dict.fromkeys(h["parent_id"] for h in hits))[:4]
context = "\n\n---\n\n".join(parents[p] for p in parent_ids)
```

Note the `k=12 → 4` shape. Over-retrieve children, then collapse — several children hitting the same parent is a *strength signal*, not redundancy.

**Budget arithmetic.** A 20,000-token manual at 256-token chunks yields ~78 children. Retrieving 12 children (3,072 tokens) versus 4 parents at 1,024 tokens (4,096 tokens) is roughly the same context spend — but the parents carry coherent, readable passages instead of 12 fragments.

Two rules that matter more than tuning the number:

1. **Split on structure first.** Recurse through `["\n## ", "\n\n", "\n", ". "]` and only fall back to a hard character cut. A chunk straddling two sections embeds as noise.
2. **Prepend the breadcrumb.** Store `"Billing > Refunds > Timing\n\n" + chunk_text`. It costs ~10 tokens and repairs most pronoun-orphan chunks for free.

Set overlap to ~15% (38 tokens on 256) — enough to save a straddled sentence, not enough to inflate the index.

---

## 15:04 - [Crypto & Business] Gross Margin: Why Your "80% SaaS Business" Is Really at 52%

Founders quote gross margin from memory ("SaaS is 80%") and then wonder why the cash never shows up. Gross margin is revenue minus **cost of goods sold** — every cost required to *deliver* the product to a customer who has already bought it. The mistake is defining COGS as "the AWS bill."

**What belongs in COGS:**
- Cloud hosting, storage, egress, CDN
- Third-party APIs and per-seat licenses you resell (LLM inference, mapping, KYC, SMS)
- Payment processing fees
- Customer support and the *reactive* portion of customer success
- Implementation and onboarding labor
- Hosting for the free tier, allocated to paid revenue

**What does not:** sales, marketing, R&D, G&A. Those sit below the line in opex.

**Worked example.** $2.0M ARR, 400 customers at $5,000/yr.

| Line | Annual | % of revenue |
|---|---:|---:|
| Revenue | $2,000,000 | 100% |
| Hosting | $180,000 | 9.0% |
| LLM API calls | $310,000 | 15.5% |
| Stripe (2.9% + $0.30) | $58,000 | 2.9% |
| Support (2 FTE loaded) | $190,000 | 9.5% |
| Onboarding (1 FTE) | $95,000 | 4.8% |
| Data/enrichment vendors | $125,000 | 6.3% |
| **Gross profit** | **$1,042,000** | **52.1%** |

That 52% is the real number, and it rewrites everything downstream. CAC payback is measured in *gross profit*, not revenue: at $6,000 CAC and $5,000 ARR, the revenue story says 14 months; the gross-profit story says $6,000 ÷ ($2,605/yr) = **28 months**. LTV on 15% annual churn drops from $33k to $17k.

**Where the levers are.** Variable costs that scale 1:1 with usage (inference, enrichment) never improve with scale — only with caching, smaller models, batching, or a usage-based price that passes the cost through. Support and onboarding *do* improve with scale, via docs, self-serve setup, and deflection. Separate the two buckets before promising the board margin expansion.

Compute it monthly from your P&L, not from an industry benchmark.

*Educational content only — not financial or investment advice.*

---

## 19:03 - [PC Hardware] USB Port Naming: What That Port on Your Motherboard Actually Does

The USB-IF has renamed the same speeds three times, so a motherboard box may advertise "USB 3.2" for a port that is functionally USB 3.0 from 2008. Connector shape tells you nothing about speed — Type-C can be 480 Mbps, and Type-A can be 10 Gbps.

Map the marketing name to the only number that matters:

| Current name | Old names | Signaling | Realistic sequential |
|---|---|---|---|
| USB 2.0 | High-Speed | 480 Mbps | ~40 MB/s |
| USB 5Gbps | 3.0, 3.1 Gen 1, 3.2 Gen 1 | 5 Gbps | ~430 MB/s |
| USB 10Gbps | 3.1 Gen 2, 3.2 Gen 2 | 10 Gbps | ~1,000 MB/s |
| USB 20Gbps | 3.2 Gen 2x2 | 20 Gbps | ~2,000 MB/s |
| USB4 / Thunderbolt 4 | — | 40 Gbps | ~3,000 MB/s |
| Thunderbolt 5 | — | 80 Gbps (120 asym.) | ~6,000 MB/s |

**Where this bites in practice.** A Gen4 NVMe drive doing 7,000 MB/s internally lands at ~1,000 MB/s in a 10 Gbps enclosure — the enclosure's interface is the ceiling, not the drive. Pay for USB4/TB only if you genuinely move large files; otherwise 10 Gbps is the value tier. Also make sure the enclosure supports **UASP** rather than legacy BOT: same link speed, but command queueing roughly doubles small-file performance.

**Front-panel headers are a separate bottleneck.** The old 19-pin internal header is 5 Gbps per port regardless of what the rear I/O offers. Only the newer 20-pin Key-A header feeds a 20 Gbps front Type-C. If your case only has the 19-pin header, no port on the front will exceed 5 Gbps.

**CPU-attached vs chipset ports.** A handful of rear ports wire directly to the CPU; the rest hang off the chipset, which shares a single x4 uplink among every chipset USB port, SATA port, and secondary M.2 slot. Saturating three chipset devices at once means they contend. Put your fastest external drive on a CPU-attached port — the motherboard block diagram in the manual says which.

Verify what you actually negotiated on Linux:

```bash
lsusb -t                          # tree view with per-device link speed
lsusb -tv | grep -E '5000M|10000M'
```

A drive showing `480M` in that tree is on a USB 2.0 link — usually a cable or a front-panel header, not the drive.
